# Marvin Solutions

These are the named solution tracks for Project Marvin.

- `bureaucratic-flow/`: Power Automate and Outlook-focused flow strategy
- `google-hub/`: OGCS with Google as the central visibility hub
- `marvin-engine/`: first-party custom sync service skeleton

Generate the shared artifacts with:

```powershell
npm run solutions:build
```
