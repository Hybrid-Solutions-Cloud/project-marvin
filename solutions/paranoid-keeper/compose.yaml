services:
  keeper:
    image: ghcr.io/ridafkih/keeper-standalone:2.9
    container_name: marvin-paranoid-keeper
    ports:
      - "3000:3000"
    volumes:
      - keeper-data:/var/lib/postgresql/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  keeper-data:
